{
  "name": "mydinnerdelights-nuxt-storyblok",
  "version": "2.0.1",
  "description": "Personal Chef in Des Moines, Iowa | Dinner Delights",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev-ssl": "nuxt dev --https --ssl-cert localhost.pem --ssl-key localhost-key.pem",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@headlessui/vue": "^1.7.23",
    "@storyblok/nuxt": "^6.2.3",
    "@vueuse/core": "^13.0.0",
    "nuxt": "^3.16.0",
    "vue": "latest"
  },
  "devDependencies": {
    "sass-embedded": "^1.85.1"
  }
}
